concatenação não funcionando
    bloco chamaProcedure(texto meuTexto , inteiro valor) {
        escreva("oi");
        escreva(meuTexto + como_texto(valor));
    }

quando se chama a funcao com 2 argumentos. Está copiando somente 1 e replicando para todos
    escreva("> Teste: O texto abaixo deveria ser: verdadeiro");
    booleano retorno = funcaoEhMaior(numero, numeroNegativo);
    escreva(retorno);
    retorno = funcaoEhMaiorQueCinco(valor);
    escreva(retorno);
